# -*- coding: utf-8 -*-
"""post_026(Threads)に2026年のあべむつき実例(20260307)を統合。誇大数字は鵜呑みにしない注記つき。"""
import json, os, io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
PD = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "posts")
fp = os.path.join(PD, "post_026.json")
d = json.load(open(fp, encoding="utf-8-sig"))
b = d["body"]

sec = """## 実例：少フォロワーでも“バズる”スレッズの強み
あるAI副業系の実例が、スレッズの強みをよく表しています。あるネイリストの方は、**インスタ約500人・スレッズ約360人**という少ないフォロワーながら、スレッズの**1投稿で42万閲覧**を記録し、そこから自分のネイル講座を販売して成果を出しました（※「5か月で90万円」とも紹介されますが、これは上澄みの一例。金額は鵜呑みにせず“仕組み”だけ参考に）。

ここから分かるスレッズの強みは3つです。

- **フォロワー数に依存しない**：「アカウントパワー」が要らず、良い投稿は少フォロワーでも一気に拡散。逆にダメだと伸びない**二極化**が特徴
- **投稿コストが激安**：テキスト主体なので、インスタのリールが週1本でも、スレッズなら**AIと組んで1日に複数投稿**できる。試行回数を稼げる
- **“学ぶ意欲の高い人”が集まる**：なんとなくフォローされるより、知りたくてフォローされるので、後の販売につながりやすい

**スレッズで伸ばす“中身”のコツ**
- **文字＋ちょっとした写真**を添えると画面の占有率が上がり、手を止めてもらいやすい
- **インスタの“キラキラ”をそのまま持ち込まない**：スレッズで刺さるのは、インスタでは伝えきれない**具体的なノウハウ・地味で詳しい中身**。表面的な見せ方だけだと埋もれます

"""
a = "## フォロワーを増やす手順"
if "少フォロワーでも“バズる”スレッズの強み" in b:
    print("実例セクション 既存 → skip")
elif a in b:
    b = b.replace(a, sec + a, 1); print("実例セクション 挿入 OK")
else:
    print("[!] アンカー不一致 → 中止"); sys.exit(1)

d["body"] = b
d["char_count"] = len(b)
d["excerpt"] = "Threads（スレッズ）集客・伸ばし方を2026年最新版で解説。2フィードとAIランキング、Xとの違い・先行者メリット、少フォロワーでも1投稿42万閲覧の実例、テキストで1日複数投稿・文字＋写真・具体ノウハウが刺さる中身のコツ、共感投稿や価値ある返信で伸ばす手順、Instagram連携、AI活用まで。誇大な金額は鵜呑みにしない注意も。"
d["notes"] = (d.get("notes") or "") + " あべむつき2026スレッズ動画(20260307)の実例を統合(2026-06-11)。少フォロワー42万閲覧・二極化・テキスト量産・具体ノウハウが刺さる。金額は鵜呑みにしない注記。"
json.dump(d, open(fp, "w", encoding="utf-8"), ensure_ascii=False, indent=2)
print("post_026 char=", d["char_count"])

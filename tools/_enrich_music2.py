# -*- coding: utf-8 -*-
"""137/138を第2次リサーチで増強。TikTok拡散術/Lo-Fi長尺ループ/誠実な現実(60%で出して続ける)/著作権訴訟タイムライン。"""
import json, os, io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
PD = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "posts")

def load(n):
    fp=os.path.join(PD,f"post_{n}.json"); return fp, json.load(open(fp,encoding="utf-8-sig"))
def ins(body, anchor, text, tag):
    if text.strip().split(chr(10))[0] in body:
        print(f"  [skip] {tag}"); return body
    if anchor not in body:
        print(f"  [!!] {tag} anchor不一致: {anchor[:34]}"); sys.exit(1)
    print(f"  [ok] {tag}")
    return body.replace(anchor, text+anchor, 1)

# ============================================================ 137
fp,d = load("137"); b=d["body"]
print("post_137:")

# TikTok拡散 + Lo-Fi長尺 → 初心者ロードマップ の前
b = ins(b, "## 初心者ロードマップ（今日からの順番）", """## TikTokで“拡散”して再生を集める
Spotifyの1,000再生の壁を越えるカギは、**TikTokでの拡散**です。AI音楽で伸びている人の多くが、**TikTok→ストリーミング誘導**で結果を出しています。アルゴリズムが見ているのは主に次の3つ。

- **初速エンゲージメント**：投稿直後の数時間の反応。最初に伸びるかが勝負
- **視聴完了率（最重要）**：最後まで見られたか。曲の“いちばんいい所（サビ・フック）”を冒頭に置く
- **アクション率**：いいね・コメント・シェア・**保存**。保存されやすい曲は強い

コツは、**流行のサウンド・フォーマットを自分流にアレンジ**すること。0から流行を作るより、伸びている波に乗る方が圧倒的に速いです。同じ曲で何本も切り口を変えて投稿し、**当たりを見つけたら一点集中**で押しましょう。

## YouTubeの“作業用BGM”で稼ぐ（Lo-Fi長尺戦略）
歌なしのBGMなら、**YouTubeの作業用BGMチャンネル**が王道です（元祖は「Lofi Girl」）。AIと相性が良く、仕組みもシンプル。

- **1〜2分のAI曲をループ**して、**30分〜1時間の長尺動画**にする
- タイトルに「**作業用／勉強用／睡眠用／集中**」などのキーワードを入れてSEOを意識
- **サムネを統一・タイトルに法則**を作り、チャンネルの世界観を固定する
- 長尺ループは**総再生時間を稼ぎやすく**、YouTubeの収益化条件（総再生時間）に貢献

ただし**競合は激しく、収益化は簡単ではありません**。最初は伸びない前提で、本数とジャンルを試しながら“当たり”を探すのが現実的です。海外リスナーを狙うなら、**英語タイトル・タグ**も添えましょう。

""", "TikTok+LoFi")

# 誠実な現実 → こんな人に向いている の前
b = ins(b, "## こんな人に向いている／向いていない", """## 【本音】稼げる人と稼げない人の差
正直に言うと、**「曲を作れること」と「稼げること」の間には深い溝**があります。AIで誰でも曲は作れるようになった分、差がつくのは“その先”です。稼げている人に共通するのは、

- **60点で出して、出し続ける**：完璧を待たず公開し、反応を見て直す。これを“日常動作”にしている
- **AIに依存せず、AIを使いこなす**：曲作りはAI任せでも、**企画・告知・改善は自分**でやる
- **プラットフォーム頼みにしない**：配信に置くだけで待つのではなく、**SNSで自分から拡散**する

逆に挫折する人は、「作って満足」「1〜2曲出して反応がなくて辞める」「規約を読まず量産だけ」のパターン。**“続けられる仕組み”を作った人**が、半年〜1年かけて収益の柱を育てています。才能ではなく、**継続と改善の差**です。

""", "本音")

d["body"]=b; d["char_count"]=len(b)
d["notes"]=(d.get("notes") or "")+" 第2次リサーチ(2026-06-13):TikTok拡散術(初速/視聴完了率/保存・流行サウンドをアレンジ)/Lo-Fi長尺ループ(1-2分曲をループ30分-1h・作業用/勉強用/睡眠用キーワード・サムネ統一・総再生時間で収益化条件)/誠実な現実(60%で出して続ける・AIを使いこなす・SNS集客が本質)。"
json.dump(d,open(fp,"w",encoding="utf-8"),ensure_ascii=False,indent=2)
print("  post_137 char=",d["char_count"])

# ============================================================ 138
fp,d = load("138"); b=d["body"]
print("post_138:")

# 著作権訴訟タイムライン → "AIっぽさ"を消す一手間 の前
b = ins(b, "## “AIっぽさ”を消す一手間", """## 【2026年】著作権訴訟は今どうなっている？（知っておくべき背景）
AI音楽を使うなら、業界の“今”も知っておくと安心です。少し大きな話ですが、あなたの曲のリスクに直結します。

- **2024年6月、レコード業界団体（RIAA／UMG・ソニー・ワーナー）がSuno・Udioを提訴**。「無断で楽曲を学習に使った」という主張で、対象は**6万曲超**とされます
- **その後、和解が進行**：**ワーナーはSunoと和解（2025年11月）**、**UMGはUdioと和解（2025年10月）**してライセンス契約へ。和解した曲は“クリーン”に使えるようになりつつあります
- **ソニーは未和解**で、**フェアユースを巡る重要判決が2026年夏に出る見込み**（2026年7月にヒアリング）。Sunoはフェアユースで争う姿勢
- この判決次第で、**AI音楽全体のルールが大きく変わる**可能性があります

何が言いたいかというと、**ルールはまだ動いている最中**だということ。だからこそ、**①有料プランで作る ②AI使用を開示する ③既存曲に似せない ④公式の最新規約を定期的に確認する**——この“守り”を徹底しておけば、ルールが変わっても慌てずに済みます。行政書士も、**有料プランで作った証拠（確認日の記録）を残すこと**を勧めています。

""", "訴訟タイムライン")

d["body"]=b; d["char_count"]=len(b)
d["notes"]=(d.get("notes") or "")+" 第2次リサーチ(2026-06-13):著作権訴訟タイムライン(RIAA/UMG・Sony・Warnerが2024/6提訴・6万曲超/Warner×Suno和解2025-11・UMG×Udio和解2025-10/Sony未和解→2026夏フェアユース判決・7月ヒアリング/Sunoは$54億評価で$4億調達)。守り4点+確認日の記録(行政書士)。"
json.dump(d,open(fp,"w",encoding="utf-8"),ensure_ascii=False,indent=2)
print("  post_138 char=",d["char_count"])
print("DONE")

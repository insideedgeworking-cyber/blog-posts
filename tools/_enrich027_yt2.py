# -*- coding: utf-8 -*-
"""post_027(X)に深掘り追加: 誰に何の価値・初期の泥臭い交流・ポストの10種類の使い分け。"""
import json, os, io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
PD = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "posts")
fp = os.path.join(PD, "post_027.json")
d = json.load(open(fp, encoding="utf-8-sig"))
b = d["body"]

sec = """## ポストの“種類”を使い分ける（伸びる人の型）
有益情報だけを投稿しても伸びません。0→1000フォロワーを最短で達成した発信者は、**目的の違うポストをバランスよく**回しています。代表的な型がこちら。

- **① 理想像を見せる**：読者がなりたい“その先の姿”を魅力的に描く
- **② 現在地→理想への道**：ロードマップや「学んで行動が大事」など、行ける道筋を見せる
- **③ シンプルに学びになる内容**：具体的なノウハウ。**いちばん拡散される**型
- **④ 肯定する**：頑張っている人を否定しない。「あなたなら行ける」と背中を押す（他ジャンルは“概念だけ”やんわり否定。個人攻撃はNG）
- **⑤ 親近感**：自虐・失敗・人間味。「この人も同じなんだ」と思ってもらう
- **⑥ 背中を見せる**：自分が実践している姿を見せる（自己投資・行動）
- **⑦ 権威性・実績**：自分の強みを“さりげなく”大きく見せて、聞く価値があると伝える

これらを混ぜることで、**学び（拡散）→ 親近感（ファン化）→ 権威性（信頼）**が積み上がり、フォローと売上につながります。

> 💡 すべての前提は「**誰に・何の価値を届けるか**」が一言で決まっていること。プロフィール1行目で「未経験者を◯◯にする人」と分かるようにし、その人（＝同じクラスター）に向けて投稿し続けるのが、0→1000の最短ルートです。

"""
a = "## フォロワー数より“売上”：マネタイズの考え方"
if "ポストの“種類”を使い分ける" in b:
    print("ポスト10種 既存 → skip")
elif a in b:
    b = b.replace(a, sec + a, 1); print("ポスト種類 挿入 OK")
else:
    print("[!] アンカー不一致"); sys.exit(1)

d["body"] = b; d["char_count"] = len(b)
d["notes"] = (d.get("notes") or "") + " yt-dlpX動画(0→1000)で深掘り追加(2026-06-13):誰に何の価値・初期の泥臭い交流・ポストの種類(理想像/道筋/学び=最拡散/肯定/親近感/背中/権威性)。"
json.dump(d, open(fp, "w", encoding="utf-8"), ensure_ascii=False, indent=2)
print("post_027 char=", d["char_count"])
